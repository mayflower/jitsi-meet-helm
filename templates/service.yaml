apiVersion: v1
kind: Service
metadata:
  name: {{ include "jitsi-meet-helm.fullname" . }}
  labels:
    {{- include "jitsi-meet-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jitsi-meet-helm.selectorLabels" . | nindent 4 }}
